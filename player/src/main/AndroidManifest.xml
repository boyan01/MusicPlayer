<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tech.summerly.quiet.player">

    <application>
        <service android:name="tech.summerly.quiet.player.service.MusicPlayerService" />

        <activity
            android:name=".service.NotificationRouterActivity"
            android:theme="@style/AppTheme.Translucent" />
    </application>

</manifest>
